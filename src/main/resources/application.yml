spring:
  profiles:
    active: dev #by default, the active profile is dev. During production, it will be replaced with prod due to environment variables
  web:
    resources:
      add-mappings: false
api:
  endpoint:
    base-url: /api/v1
server:
  port: 80